# EduGenie AI Service — Python dependencies
# Install with: pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
httpx>=0.27.0

# ── ML / AI ────────────────────────────────────────────────────
torch>=2.2.0
transformers>=4.44.2
accelerate>=0.30.0
bitsandbytes>=0.43.0
peft>=0.11.0
sentencepiece>=0.2.0
sentence-transformers>=2.7.0

# ── TTS ────────────────────────────────────────────────────────
edge-tts>=6.1.0
pydub>=0.25.1

# ── Slides / PPTX ─────────────────────────────────────────────
python-pptx>=1.0.0

# ── Video / image ──────────────────────────────────────────────
moviepy>=1.0.3
pdf2image>=1.17.0
# Pillow is a transitive dep of the above; pin for safety
Pillow>=10.3.0

# ── NLP utilities ──────────────────────────────────────────────
nltk>=3.8.1
yake>=0.4.8

# ── Optional: SDXL images (comment out if not needed) ─────────
# diffusers>=0.27.0

# ── Datasets (needed if you want to re-train quiz model) ──────
datasets>=2.20.0
evaluate>=0.4.1
rouge-score>=0.1.2

# ── Utilities ──────────────────────────────────────────────────
numpy>=1.26.0
